[project]
name = "budgetthingy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "Pillow>=10.0",
    "python-dotenv>=1.0",
    "vercel>=0.3",
]

[tool.vercel]
entrypoint = "api.index:handler"
